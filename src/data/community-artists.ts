import type { Artist } from './artists';

/**
 * @deprecated This file is no longer in use. Datasets are now managed in the
 * `src/data/datasets` directory and processed in `src/data/artists.ts`.
 * See the README.md for contribution guidelines.
 */
export const communityArtists: Artist[] = [];
